server:
  port: 8091

spring:
  application:
    name: wheatmall-product
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.8.112:8848
        namespace: public
        group: DEFAULT_GROUP
        register-enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.8.112:5432/wheatmall_pms
    username: faris
    password: "123456"

logging:
  file:
    path: /tmp  # 所有日志（包括Nacos）都会输出到 /tmp 目录
  level:
    com.wheatmall.product: debug
