server:
  port: 8096

spring:
  application:
    name: wheatmall-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public
        group: DEFAULT_GROUP
        register-enabled: true

logging:
  file:
    path: /tmp
  level:
    com.wheatmall.auth: debug
